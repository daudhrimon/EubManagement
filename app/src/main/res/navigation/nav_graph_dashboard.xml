<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_dashboard"
    app:startDestination="@id/home">

    <fragment
        android:id="@+id/home"
        android:name="com.polok.eubmanagement.presentation.dashboard.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/chat"
        android:name="com.polok.eubmanagement.presentation.dashboard.chat.ChatListFragment"
        android:label="Chat"
        tools:layout="@layout/fragment_chat_list"/>

    <fragment
        android:id="@+id/profile"
        android:name="com.polok.eubmanagement.presentation.dashboard.profile.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile"/>

</navigation>